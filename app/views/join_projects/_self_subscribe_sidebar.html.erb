<h3><%= l(:join_project_text_join_project) %></h3>

<p>
  <%= link_to(l(:join_project_text_join_this_project), join_projects_path(:project_id => project, :back_url => url_for(params)), :method => :post) %>
</p>
